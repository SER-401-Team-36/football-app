version: "3.8"
services:
  db:
    image: postgres:12-alpine
    volumes:
      - /var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: example
  web:
    image: football-app
    ports:
    - target: 5000
      published: 5000
    environment:
      DATABASE_URI: postgres://db 
      FLASK_APP: app